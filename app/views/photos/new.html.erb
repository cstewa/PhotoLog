<h1>Make A New Photo</h1>


<%= form_for(@photo, :url => "/albums/#{@album.id}/photos", :html_options => {:multipart => true}) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :picture %>
	<%= f.file_field :picture %>
	<%= f.submit "Add New Photo" %>
<% end %>
